version: '3.4'

services:
  rockstor:
    build: .
    privileged: true
    hostname: rockstor
    network_mode: host
    tmpfs: /run
    volumes:
      - /sys/fs/cgroup:/sys/fs/cgroup:ro
      - /var/run/docker.sock:/var/run/docker.sock
